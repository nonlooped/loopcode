{
  "name": "loopcode",
  "private": true,
  "version": "1.0.1",
  "description": "LoopCode — local AI coding agent",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "lint": "tsc --noEmit && cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings",
    "test": "tsc --noEmit && vitest run && cargo test --manifest-path src-tauri/Cargo.toml && node scripts/check-capabilities.mjs && npm run sbom:check",
    "test:unit": "cargo test --manifest-path src-tauri/Cargo.toml",
    "test:web": "vitest run",
    "test:web:watch": "vitest",
    "sbom": "node node_modules/@cyclonedx/cdxgen/bin/cdxgen.js --type nodejs --type rust --recurse --no-install-deps --fail-on-error --validate --spec-version 1.6 --output sbom/loopcode-sbom.json .",
    "sbom:check": "npm run sbom && node scripts/check-sbom-licenses.mjs",
    "release-gate": "npm test && cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings && vite build",
    "catalog:refresh": "node scripts/refresh-models-catalog.mjs"
  },
  "dependencies": {
    "@base-ui/react": "^1.7.0",
    "@lobehub/icons-static-svg": "^1.94.0",
    "@streamdown/code": "^1.1.1",
    "@tabler/icons-react": "^3.46.0",
    "@tanstack/react-query": "^5.101.4",
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2.7.2",
    "diff": "^9.0.0",
    "motion": "^12.42.2",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "streamdown": "^2.5.0",
    "zustand": "^5.0.14"
  },
  "devDependencies": {
    "@cyclonedx/cdxgen": "^12.8.2",
    "@tailwindcss/vite": "^4.3.3",
    "@tauri-apps/cli": "^2",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "@vitejs/plugin-react": "^6.0.5",
    "babel-plugin-react-compiler": "^1.0.0",
    "tailwindcss": "^4.3.2",
    "typescript": "~7.0.2",
    "vite": "^8.2.1",
    "vitest": "^4.1.10"
  },
  "allowScripts": {
    "esbuild@0.25.12": true
  }
}
