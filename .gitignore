# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies & build
node_modules
dist
dist-ssr
dist-release
*.local
src-tauri/target
src-tauri/gen

# SBOM output (regenerated by npm run sbom; optional to commit)
# Keep directory structure via .gitkeep if needed — artifact itself is CI-produced
sbom/*.json

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
Thumbs.db
Desktop.ini

# Env / secrets
.env
.env.*
!.env.example
legacy-ref/

# Runtime scratch (terminal sessions, etc.)
terminals/
.impeccable/live/sessions/

# Agent tooling / dev scratch (not part of the app)
.agents/
.claude/
.impeccable/
.vscode/
archive/
mcps/
plans/
CLAUDE.md
AGENTS.md
skills-lock.json

# Windows reserved device names (accidental redirect artifacts)
/nul
/NUL

